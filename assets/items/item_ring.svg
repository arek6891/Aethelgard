<svg width="64" height="64" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <filter id="shadow">
      <feDropShadow dx="2" dy="2" stdDeviation="1" flood-color="#000" flood-opacity="0.5"/>
    </filter>
  </defs>
  <!-- Band -->
  <circle cx="32" cy="32" r="12" fill="none" stroke="#FFD700" stroke-width="4" filter="url(#shadow)"/>
  <!-- Gem -->
  <circle cx="32" cy="20" r="4" fill="#DC143C" stroke="#8B0000" stroke-width="1"/>
</svg>
